[Unit]
Description=Archon nightly SQLite backup

[Service]
Type=oneshot
User=ubuntu
ExecStart=/bin/bash -c 'sqlite3 /opt/archon/data/archon.db ".backup /opt/archon/backups/archon-$(date +%%F).db" && ls -1t /opt/archon/backups/archon-*.db | tail -n +8 | xargs -r rm --'
